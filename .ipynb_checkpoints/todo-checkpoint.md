# Todo:
-modify code to remove all host galaxy apertures
-Edit decomposition.py to make the main script a callable function with the object list, various paths and settings (such as excluding the host component) a parameter of the function to avoid editing the script each time